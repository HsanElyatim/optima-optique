import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ['example.com', "images.pexels.com", "ayhglndwcogqpesqcqmc.supabase.co"], // ← create your image domain(s) here
    },
};

export default nextConfig;
